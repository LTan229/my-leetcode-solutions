---
Tags:
  - SQL
  - 聚合函数
Languages:
  - SQL
URL: https://leetcode.cn/problems/monthly-transactions-i/description/?envType=study-plan-v2&envId=sql-free-50
---
```SQL
# Write your MySQL query statement below

select query_name, ROUND(AVG(rating / position), 2) as quality, ROUND(SUM(rating < 3) * 100 / COUNT(*), 2) as poor_query_percentage
from Queries
group by query_name
```