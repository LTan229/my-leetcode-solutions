---
Tags:
  - SQL
  - 高级查询和连接
Languages:
  - SQL
URL: https://leetcode.cn/problems/consecutive-numbers/?envType=study-plan-v2&envId=sql-free-50
---
```SQL
select distinct a.num as ConsecutiveNums
from Logs as a left join Logs as b on a.id + 1 = b.id left join Logs as c on a.id + 2 = c.id
where a.num = b.num and b.num = c.num
```