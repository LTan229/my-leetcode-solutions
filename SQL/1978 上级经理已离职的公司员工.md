---
Tags:
  - SQL
  - 子查询
Languages:
  - SQL
URL: https://leetcode.cn/problems/employees-whose-manager-left-the-company/?envType=study-plan-v2&envId=sql-free-50
---
```SQL
select e.employee_id
from Employees as m right join Employees as e on m.employee_id = e.manager_id
where m.employee_id is null and e.salary < 30000 and e.manager_id is not null
order by e.employee_id 
```