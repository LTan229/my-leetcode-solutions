---
Tags:
  - SQL
  - 字符串函数
Languages:
  - SQL
URL: https://leetcode.cn/problems/fix-names-in-a-table/?envType=study-plan-v2&envId=sql-free-50
---
```SQL
select user_id,
CONCAT(UPPER(SUBSTRING(name, 1, 1)), 
    LOWER(SUBSTRING(name, 2))) as name
from Users
order by user_id
```