---
Tags:
  - SQL
  - 连接
Languages:
  - SQL
URL: https://leetcode.cn/problems/confirmation-rate/description/?envType=study-plan-v2&envId=sql-free-50
---
```SQL
# Write your MySQL query statement below
select 
	Signups.user_id, 
	ROUND(
		IFNULL(SUM(Confirmations.action = 'confirmed') / 
	    Count(Confirmations.action),0)
	,2) 
as confirmation_rate
from Signups
left join Confirmations
on Signups.user_id = Confirmations.user_id
group by Signups.user_id
```