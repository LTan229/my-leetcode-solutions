---
Tags:
  - SQL
  - 高级查询和连接
Languages:
  - SQL
URL: https://leetcode.cn/problems/triangle-judgement/?envType=study-plan-v2&envId=sql-free-50
---
```SQL
select *, (
    CASE
    when x + y > z and x + z > y and y + z > x then 'Yes' else 'No'
    END
) as triangle
from Triangle
```