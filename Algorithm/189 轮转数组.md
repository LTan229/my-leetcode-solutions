---
Tags:
  - 数组
  - 数学
  - 双指针
  - H100
Languages:
  - Python
URL: https://leetcode.cn/problems/rotate-array/submissions/721649416/?envType=study-plan-v2&envId=top-100-liked
---
# 直觉：字典

空间复杂度 On

```python
class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        k = k % n
        idx2num = {}
        for i in range(n):
            idx2num[(i + k) % n] = nums[i]
        for i in range(n):
            nums[i] = idx2num[i]
```

# 数组切片

```python
class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        k = k % n
        nums[:] = nums[n - k:] + nums[:n - k]
```

# 三次翻转

```python
class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        k = k % n
        nums[:] = nums[::-1]
        nums[:k] = nums[:k][::-1]
        nums[k:] = nums[k:][::-1]

```

# 模拟

```Python
class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        k %= n
        count = 0
        start = 0
        while count < n:
            curr = start
            prev_val = nums[start]
            while True:
                next_idx = (curr + k) % n
                nums[next_idx], prev_val = prev_val, nums[next_idx]
                curr = next_idx
                count += 1
                if start == curr:
                    break
            start += 1
```