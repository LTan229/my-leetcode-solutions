---
Tags:
  - 数组
  - 双指针
  - 排序
Languages:
  - Python
URL: https://leetcode.cn/problems/4sum/
---
# 排序双指针
```python
class Solution:
    def fourSum(self, nums: List[int], target: int) -> List[List[int]]:
        lennums = len(nums)
        if lennums < 4:
            return []
        
        nums.sort()
        results = []
        for i in range(lennums):
            if i != 0 and nums[i] == nums[i - 1]:
                continue
            for j in range(i + 1, lennums):
                if j != i + 1 and nums[j] == nums[j - 1]:
                    continue
                left = j + 1
                right = lennums - 1
                while left < right:
                    sums = nums[i] + nums[j] + nums[left] + nums[right]
                    if sums == target:
                        results.append([nums[i], nums[j], nums[left], nums[right]])
                    if not sums > target: # <= move left
                        left += 1
                        while left < right and nums[left] == nums[left - 1]:
                            left += 1
                    if not sums < target: # >= move right
                        right -= 1
                        while left < right and nums[right] == nums[right + 1]:
                            right -= 1
        
        return results
```

