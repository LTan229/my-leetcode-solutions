---
Tags:
  - 树
  - DFS
  - 二叉树
  - H100
Languages:
  - Python
URL: https://leetcode.cn/problems/diameter-of-binary-tree/description/?envType=study-plan-v2&envId=top-100-liked
---

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:
        self.maxlen = 0
        self.getLegLens(root)
        return self.maxlen
    
    def getLegLens(self, root):
        if not root.left and not root.right:
            return 0
        left_len, right_len = 0, 0
        if root.left:
            left_len = self.getLegLens(root.left) + 1
        if root.right:
            right_len = self.getLegLens(root.right) + 1
        self.maxlen = max(self.maxlen, left_len + right_len)
        return max(left_len, right_len)

```


```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:
        self.maxlen = 0
        self.getLegLens(root)
        return self.maxlen
    
    def getLegLens(self, root):
        if not root:
            return 0
        left_len = self.getLegLens(root.left)
        right_len = self.getLegLens(root.right)
        self.maxlen = max(self.maxlen, left_len + right_len)
        return max(left_len, right_len) + 1
```