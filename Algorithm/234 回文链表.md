---
Tags:
  - 栈
  - 递归
  - 链表
  - 双指针
Languages:
  - Python
URL: https://leetcode.cn/problems/palindrome-linked-list/description/?envType=study-plan-v2&envId=top-100-liked
---
# 递归

又耗时又耗内存

```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        def method(backnode, frontnode, ispalindrome):
            if not backnode.next:
                return backnode, head, backnode.val == head.val
            back_next, front_prev, next_is = method(backnode.next, frontnode, True)
            cur_is = backnode.val == front_prev.next.val and next_is
            return backnode, front_prev.next, cur_is
        
        _, _, result = method(head, None, True)
        return result

            
```

## 递归类变量

```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        self.head = head

        def recursive(cur):
            if cur: # handles tail
                if not recursive(cur.next): # not (next is tail or next is palindrome)
                    return False
                if self.head.val != cur.val: # cur is not palindrome
                    return False
                self.head = self.head.next
            return True

        return recursive(head)

            
```

# 栈

```python
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        stack = []
        curr = head
        while curr:
            stack.append(curr.val)
            curr = curr.next
        
        curr = head
        while curr:
            if curr.val != stack.pop():
                return False
            curr = curr.next
        return True

```

# 双指针

```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        slow, fast = head, head
        while fast.next and fast.next.next:
            slow = slow.next
            fast = fast.next.next
        
        pre = None
        cur = slow.next # so that the reversed half is either half or half excluding the midpoint
        while cur:
            next_ = cur.next
            cur.next = pre
            pre = cur
            cur = next_
        
        while pre: # match the reversed half because the midpoint is in the head part
            if pre.val != head.val:
                return False
            pre = pre.next
            head = head.next
        
        return True

```

# 双指针+栈

- 快慢指针找中点，慢指针边走边入栈
- 然后慢指针边走边出栈
	- 如果是偶数长度（fast.next = None），则慢指针开始位置需要对比
	- 如果是奇数长度，则慢指针开始位置（中点）不参与对比