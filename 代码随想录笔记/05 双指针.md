快指针找符合要求的对象，慢指针维护符合要求的对象

# 数组
[[27 移除元素]]
[[977 有序数组的平方]]
[[26 删除有序数组中的重复项]]
[[283 移动0]]

# 字符串
[[344 反转字符串]] 定义两个指针（也可以说是索引下标），一个从字符串前面，一个从字符串后面，两个指针同时向中间移动，并交换元素。
[[54 替换数字]] 首先扩充数组到每个空格替换成"%20"之后的大小。然后双指针从后向前替换空格。
[[151 反转字符串]] erase是O(n2)

[[844 比较含退格的字符串]] ⭐ 从后向前遇到退格则遍历这一组删除、执行足够数量的删除、如果都有效则比较。

# 链表
[[206 反转链表]] 只需要改变链表的next指针的指向，直接将链表反转 ，而不用重新定义一个新的链表。
[[142 环形链表 II]] 分别定义 fast 和 slow指针，从头结点出发，fast指针每次移动两个节点，slow指针每次移动一个节点，如果 fast 和 slow指针在途中相遇 ，说明这个链表有环。从开头和相遇点同时出发两个指针，在环入口相遇
[[19 删除链表的倒数第 N 个结点]]
[[02.07 链表相交]]

# N数和
[[15 三数之和]] 通过前后两个指针不断向中间逼近，在一个for循环下完成两个for循环的工作。
[[18 四数之和]] 在三数之和的基础上再套一层for循环，依然是使用双指针法。
