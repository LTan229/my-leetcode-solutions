用来快速判断一个元素是否出现集合里O（1）
哈希函数：把数据映射到哈希表上，取模
哈希碰撞：两个数据映射到一个索引
	拉链法：在索引处储存链表
	线性探测：在索引后续空位放数
哈希结构：
	数组（array）：值范围有限，如26个字母
	set（哈希表）：数值范围大，较为稀疏
	map：不仅需要值还需要下标位置

# 数组作为哈希表
[[242 有效的字母异位词]]
[[383 赎金信]]

# set作为哈希表
[[349 两个数组的交集]]
[[202 快乐数]]

# map作为哈希表
不用考虑重复
[[1 两数之和]] 找下标。把元素和下标存到 map 里，排序之后二分搜索；正解：建立 map，n->idx。如果 target - n 在数组里则直接返回，否则 n 加入数组
[[454 四数相加 II]] 找次数。不用考虑重复。首先遍历 AB 的所有组合，把和的出现次数储存在哈希表里，然后遍历 CD 的所有组合，去哈希表里找匹配的组合

# 双指针
要去重（map 去重很慢），排序，利用有序性跳过重复
[[15 三数之和]] 不重复下标。双指针：排序，一个指针指向考虑的第一个数 i，left->i+1, right-> -1.如果 left + right + i > 0, right -- 。如果等于 0,则记录，如果小于 0, left ++
[[18 四数之和]] 不重复找下标。双指针：两层 for 循环 k, j 为确定职，在此基础上右侧做双指针

考虑次数
[[350 两个数组的交集 II]] 排序 双指针



