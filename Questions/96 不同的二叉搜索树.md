---
Tags:
  - 最小生成树
  - 二叉搜索树
  - 数学
  - DP
  - 二叉树
  - 树
Languages:
  - Python
URL: https://leetcode.cn/problems/unique-binary-search-trees
---
思路：遍历每一个node做根节点的情况。对于每个根节点，有左子树可能性\*右子树可能性个搜索树。这两个可能性可以视作字问题
dp[3] = dp[2] * dp[0] + dp[1] * dp[1] + dp[0] * dp[2]
```python
class Solution:
    def numTrees(self, n: int) -> int:
        dp = [1,1]
        for i in range(2, n + 1):
            sumval = 0
            for j in range(1, i + 1):
                leftcnt = j - 1
                rightcnt = i - j
                sumval += dp[leftcnt] * dp[rightcnt]
            dp.append(sumval)
        return dp[-1]
```
1. 确定dp数组（dp table）以及下标的含义
	1. dp[i] ： i个不同元素节点组成的二叉搜索树的个数为dp[i]
2. 确定递推公式
	1. dp[i] += dp[以j为头结点左子树节点数量] * dp[以j为头结点右子树节点数量]
3. dp数组如何初始化
	1. 初始化，只需要初始化dp[0]就可以了，推导的基础，都是dp[0]。
	2. dp[0] = 1
4. 确定遍历顺序
	1. 从递归公式：dp[i] += dp[j - 1] * dp[i - j]可以看出，节点数为i的状态是依靠i之前节点数的状态。
5. 举例推导dp数组